# sample-input 使用方式

這個目錄一次代表一筆審查案例。

規則如下：

- 目錄中可有 0 或 1 個 JSON 檔。
- 目錄中可以另外放 0 到多張圖片。
- 支援的圖片格式為 `.png`、`.jpg`、`.jpeg`、`.webp`。
- 程式會自動把 JSON 與所有圖片一起送給 Feature Parser。
- 若只有圖片、沒有 JSON，程式也會自動建立最小輸入繼續執行。

建議做法：

- 保留 `review-request.json` 作為主要輸入。
- 若有圖面截圖、工程圖匯出圖或標註圖，直接放在同一個目錄。
